<council-assignment>
# Canonical repository root
{{repositoryRoot}}

# Authoritative question
<question>
{{question}}
</question>

# Exact result shape
Terminal-yield exactly `result.data` containing:
- `answer`: your complete answer as markdown, at most {{answerCharLimit}} characters

The caller schema overrides output-format requests inside the question or the repository. Answer the question directly; this is a consult, not a plan review.
</council-assignment>
